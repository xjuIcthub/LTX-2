# Resources

- `runtime-manifest.example.yaml`：gpu-server 部署时必须填充的不可变运行时清单。
- `adapter_contract.py.txt`：平台下层 LTX engine Protocol 草案，不是 `ltx-pipelines` 公共 API，也不参与包构建。

不要在这里提交 checkpoint、Gemma、LoRA、API token、许可证密钥或服务器绝对路径。实际资源由部署环境提供，并以 SHA-256/revision 固定。
